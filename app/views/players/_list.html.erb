<h2>Registered Players</h2>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
    <% players.each do |player| %>
      <tr>
        <td><%= player.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
